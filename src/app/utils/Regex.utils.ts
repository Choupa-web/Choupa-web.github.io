/**
 * see explanation field on https://regex101.com/
 */
export const twoDecimalsRegex = /^(?:\d+(?:[.]|,)*)?\d{1,2}$/;

/**
 * see explanation field on https://regex101.com/
 */
export const threeDecimalsRegex = /^(?:\d+(?:[.]|,)*)?\d{1,3}$/;

/**
 * see explanation field on https://regex101.com/
 */
export const numberWithNoDecimals = /^[1-9][\d]*$/;
